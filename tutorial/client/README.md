# Content Client

This folder holds the client code for the content side of the tutorial.

## Run locally

Install dependencies and start the dev server from tutorial/client folder:

```bash
npm install
npm run dev
```
